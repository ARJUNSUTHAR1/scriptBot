import { atom } from 'recoil';

// export const authTokenState = atom({
//   key: 'authTokenState',
//   default: null, 
// });

export const InfoState = atom({
  key: 'InfoState',
  default: true, 
});